pipeline {
    agent any  // This lets the pipeline run on any available agent

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, World! 2'
            }
        }
    }
}
